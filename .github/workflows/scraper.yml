name: Daily RSS Scraper

on:
  schedule:
    # Run every 6 hours
    - cron: '0 */6 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  scrape-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Needed to create/update releases
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Determine Year and File
        id: date_info
        run: |
          TODAY=$(date -u +"%Y-%m-%d")
          YEAR=$(date -u +"%Y")
          echo "today=$TODAY" >> $GITHUB_OUTPUT
          echo "year=$YEAR" >> $GITHUB_OUTPUT

      - name: Restore Previous State (Upsert Prep)
        run: |
          mkdir -p scripts/output
          # Try to download today's existing JSON so we don't overwrite it
          wget -q https://github.com/${{ github.repository }}/releases/download/rss-data-${{ steps.date_info.outputs.year }}/${{ steps.date_info.outputs.today }}.json -O scripts/output/${{ steps.date_info.outputs.today }}.json || echo "No existing data for today."
          # Try to download the global index to prevent cross-day duplicates
          wget -q https://github.com/${{ github.repository }}/releases/download/rss-data-${{ steps.date_info.outputs.year }}/index.json -O scripts/output/index.json || echo "No existing index found."

      - name: Run Scraper
        run: npm run scrape

      - name: Publish to Yearly Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: rss-data-${{ steps.date_info.outputs.year }}
          name: RSS Data Archive ${{ steps.date_info.outputs.year }}
          files: |
            scripts/output/${{ steps.date_info.outputs.today }}.json
            scripts/output/index.json
          fail_on_unmatched_files: true
          make_latest: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
