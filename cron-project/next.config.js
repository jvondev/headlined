/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special configurations needed for a simple API-only project
  // Vercel will automatically detect and build API routes in pages/api
};

module.exports = nextConfig;
